# Open questions

- Which Prompt IR fields should become first-class next: examples, source provenance, tool permissions, or grader criteria?
- Which representative task datasets should anchor v0.3 model-backed evaluation?
- Should provider-specific compiler adapters be introduced before or after local prompt versioning?
- What minimum evidence should permit the UI to label a candidate “improved” rather than merely “generated”?
- When GitHub Actions access is restored, should main receive branch protection requiring the CI check for pull requests?
